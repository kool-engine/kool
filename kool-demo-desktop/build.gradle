apply plugin: 'kotlin'

sourceSets {
    main.kotlin.srcDirs += "src/main/kotlin"
}

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    compile project(":kool-core")
    compile project(":kool-desktop")
    compile project(":kool-demo")
}
