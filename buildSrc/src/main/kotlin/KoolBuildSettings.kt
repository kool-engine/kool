object KoolBuildSettings {

    /**
     * If true languageVersion = "2.0" is used. This works but currently often requires clean.
     */
    val useK2 = false

    /**
     * Only affects javascript output of kool-demo (development vs. release)
     */
    val isRelease = false

}